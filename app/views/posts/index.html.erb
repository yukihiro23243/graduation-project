<h1>記録一覧</h1>

<% @posts.each do |post| %>
  <div>
    <h3><%= post.title %></h3>
    <p><%= post.content %></p>
    <p>投稿者：<%= post.user.email %></p>
    
    <% if post.image.attached? %>
      <%= image_tag post.image %>
    <% end %>
  </div>
<% end %>