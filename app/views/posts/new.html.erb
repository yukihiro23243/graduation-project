<h1>記録作成</h1>

<%= form_with model: @post do |f| %>
  <p>サウナ名</p>
  <%= f.text_field :title %>

  <p>内容</p>
  <%= f.text_area :content %>

  <p>画像</p>
  <%= f.file_field :image %>

  <%= f.submit "投稿する" %>
<% end %>